# TODO

* Make sure the servers are adding dynamically, like port, ip. For sending request to the correct server sending type(FHIR | HL7).
* Remove CNIC from every tables.
* MPI(Master Patient Index) should be use in everytable.

# Removed
* primary key pid from EHR, lab, and health insurace
* cnic from all databases